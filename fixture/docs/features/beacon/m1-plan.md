# Beacon M1 — Signal contract

Fixture content only, invented for Atlas's own test suite (decision 40: the generator holds no
project content of its own). This plan exists so Beacon's M1 milestone points at a real file.

Ship the shape every later Beacon milestone builds on: what a "signal" is and how the lamp
rotation service in M2 will consume it.

- [x] Define the signal contract
- [x] Wire it into the fixture's demo script
