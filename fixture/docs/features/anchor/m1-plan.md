# Anchor M1 — Chain and windlass contract

Fixture content only, invented for Atlas's own test suite (decision 40: the generator holds no
project content of its own). This plan exists so Anchor's M1 milestone points at a real file.

Ship the shape every later Anchor milestone builds on: what a "rode" is and how the tension
telemetry in M2 will consume it.

- [x] Define the chain and windlass contract
- [x] Wire it into the fixture's demo script
