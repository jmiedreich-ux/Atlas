{
  "name": "atlas",
  "version": "0.0.0",
  "description": "Atlas — a reusable generator that turns a project repository into a static site.",
  "type": "module",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "markdown-it": "^15.0.0"
  }
}
